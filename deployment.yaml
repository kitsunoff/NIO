apiVersion: v1
kind: ServiceAccount
metadata:
  name: nixos-operator
  namespace: nixos-operator-system
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: nixos-operator
rules:
- apiGroups: ["nio.homystack.com"]
  resources: ["machines", "nixosconfigurations"]
  verbs: ["get", "list", "watch", "create", "update", "patch", "delete"]
- apiGroups: ["nio.homystack.com"]
  resources: ["machines/status", "nixosconfigurations/status"]
  verbs: ["get", "update", "patch"]
- apiGroups: [""]
  resources: ["secrets"]
  verbs: ["get", "list", "watch"]
- apiGroups: [""]
  resources: ["events"]
  verbs: ["create", "patch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: nixos-operator
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: nixos-operator
subjects:
- kind: ServiceAccount
  name: nixos-operator
  namespace: nixos-operator-system
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nixos-operator
  namespace: nixos-operator-system
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nixos-operator
  template:
    metadata:
      labels:
        app: nixos-operator
    spec:
      serviceAccountName: nixos-operator
      containers:
      - name: operator
        image: ghcr.io/homystack/nio:main
        imagePullPolicy: IfNotPresent   # ←←← ЭТО ОБЯЗАТЕЛЬНО ДЛЯ :latest

        env:
        - name: KUBERNETES_SERVICE_HOST
          value: "kubernetes.default.svc"
        - name: KUBERNETES_SERVICE_PORT
          value: "443"
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        securityContext:
          runAsUser: 1000
          runAsGroup: 1000
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
